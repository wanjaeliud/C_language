#include <stdio.h>

int main(void)
{
        int a, b;
        printf("Enter the first value\n");
        scanf("%d", &a);
        printf("Enter the second value\n");
        scanf("%d", &b);
        printf("%d + %d = %d", a, b, a + b);
        return (0);
}
